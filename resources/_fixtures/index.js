'use strict';

const imageBase64 = require('./imageBase64.json').base64;
const imageBuffer = require('./imageBuffer.json').buffer;
const thumbnailBase64 = require('./thumbnailBase64.json').base64;

module.exports = {
    imageBase64,
    imageBuffer,
    thumbnailBase64
};